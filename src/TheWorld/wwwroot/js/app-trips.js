// app-trips.js
// Trip view of the app controller

// immediately invoking function expression, purpose is to keep out of global scope

(function () {
  "use strict";

  //Specify a module where the code lives.   Creating it -- that's what the [] means
  angular.module("app-trips", ["simpleControls"]);

}
)();